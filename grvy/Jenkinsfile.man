node('master'){
  environment {
        PREPROD_BUCKET = "dialogflow-bucket-preprod"
        PROJECT_NAME = "prerprod-project"
        LOCATION = "asia-northeast1"
    }
  stage('preparation'){
         def s = load"${WORKSPACE}/grvy/Jenkinsfile.gcpComponentUpdate"  
         s.gcp()
  }
  stage('workspace cleanup'){
        cleanWs()
}
}
